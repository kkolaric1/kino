{% extends 'base.html.twig' %}

{% block title %}Korisnik{% endblock %}

{% block body %}
    <h1>Korisnik</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdKorisnik</th>
                <td>{{ korisnik.idKorisnik }}</td>
            </tr>
            <tr>
                <th>Ime</th>
                <td>{{ korisnik.ime }}</td>
            </tr>
            <tr>
                <th>Prezime</th>
                <td>{{ korisnik.prezime }}</td>
            </tr>
            <tr>
                <th>UlogaIdUloga</th>
                <td>{{ korisnik.ulogaIdUloga }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('korisnik_index') }}">back to list</a>

    <a href="{{ path('korisnik_edit', {'idKorisnik': korisnik.idKorisnik}) }}">edit</a>

    {{ include('korisnik/_delete_form.html.twig') }}
{% endblock %}
