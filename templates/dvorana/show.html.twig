{% extends 'base.html.twig' %}

{% block title %}Dvorana{% endblock %}

{% block body %}
    <h1>Dvorana</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdDvorana</th>
                <td>{{ dvorana.idDvorana }}</td>
            </tr>
            <tr>
                <th>BrojSjedala</th>
                <td>{{ dvorana.brojSjedala }}</td>
            </tr>
            <tr>
                <th>Naziv</th>
                <td>{{ dvorana.naziv }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dvorana_index') }}">back to list</a>

    <a href="{{ path('dvorana_edit', {'idDvorana': dvorana.idDvorana}) }}">edit</a>

    {{ include('dvorana/_delete_form.html.twig') }}
{% endblock %}
