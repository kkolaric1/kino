{% extends 'base.html.twig' %}

{% block title %}Film{% endblock %}

{% block body %}
    <h1>Film</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdFilm</th>
                <td>{{ film.idFilm }}</td>
            </tr>
            <tr>
                <th>Naziv</th>
                <td>{{ film.naziv }}</td>
            </tr>
            <tr>
                <th>ZanroviIdZanr</th>
                <td>{{ film.zanroviIdZanr }}</td>
            </tr>
            <tr>
                <th>Trajanje</th>
                <td>{{ film.trajanje }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('film_index') }}">back to list</a>

    <a href="{{ path('film_edit', {'idFilm': film.idFilm}) }}">edit</a>

    {{ include('film/_delete_form.html.twig') }}
{% endblock %}
