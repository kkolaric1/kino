{% extends 'base.html.twig' %}

{% block title %}Projekcija index{% endblock %}

{% block body %}
    <h1>Projekcija index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdProjekcija</th>
                <th>Vrijeme</th>
                <th>BrojKarata</th>
                <th>FilmoviIdFilm</th>
                <th>DvoraneIdDvorana</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for projekcija in projekcijas %}
            <tr>
                <td>{{ projekcija.idProjekcija }}</td>
                <td>{{ projekcija.vrijeme ? projekcija.vrijeme|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ projekcija.brojKarata }}</td>
                <td>{{ projekcija.filmoviIdFilm }}</td>
                <td>{{ projekcija.dvoraneIdDvorana }}</td>
                <td>
                    <a href="{{ path('projekcija_show', {'idProjekcija': projekcija.idProjekcija}) }}">show</a>
                    <a href="{{ path('projekcija_edit', {'idProjekcija': projekcija.idProjekcija}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('projekcija_new') }}">Create new</a>
{% endblock %}
