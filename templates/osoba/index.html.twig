{% extends 'base.html.twig' %}

{% block title %}Osoba index{% endblock %}

{% block body %}
    <h1>Osoba index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdOsoba</th>
                <th>Ime</th>
                <th>Prezime</th>
                <th>ProdukcijskaUlogaIdProdukcijskauloga</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for osoba in osobas %}
            <tr>
                <td>{{ osoba.idOsoba }}</td>
                <td>{{ osoba.ime }}</td>
                <td>{{ osoba.prezime }}</td>
                <td>{{ osoba.produkcijskaUlogaIdProdukcijskauloga }}</td>
                <td>
                    <a href="{{ path('osoba_show', {'idOsoba': osoba.idOsoba}) }}">show</a>
                    <a href="{{ path('osoba_edit', {'idOsoba': osoba.idOsoba}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('osoba_new') }}">Create new</a>
{% endblock %}
