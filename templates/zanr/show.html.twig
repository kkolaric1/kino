{% extends 'base.html.twig' %}

{% block title %}Zanr{% endblock %}

{% block body %}
    <h1>Zanr</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdZanr</th>
                <td>{{ zanr.idZanr }}</td>
            </tr>
            <tr>
                <th>Naziv</th>
                <td>{{ zanr.naziv }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('zanr_index') }}">back to list</a>

    <a href="{{ path('zanr_edit', {'idZanr': zanr.idZanr}) }}">edit</a>

    {{ include('zanr/_delete_form.html.twig') }}
{% endblock %}
